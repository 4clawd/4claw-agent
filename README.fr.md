<div align="center">

  <h1>4claw : Assistant IA Ultra-Efficace en Go</h1>

  <h3>Mat閼煎嵐iel 閼?10$ 鐠?10 Mo de RAM 鐠?D閼煎崻arrage en 1s 鐠?闁活煈鍠氬В濠囨惞閹惧懐绀夐柟瀛樺灣濠婃垹鎸х敮顔剧＜</h3>


 [濞戞搩鍘介弸鍍?README.zh.md) | [闁哄啨鍎插﹢鎵嚔閻?README.ja.md) | [Portugu闁挎](README.pt-br.md) | [Ti瀹€椋庝刊g Vi瀹勫嫬妾糫(README.vi.md) | [English](README.md) | **Fran閼剧禈is**
</div>

---

妫ｅ喚娴€ **4claw** est un assistant personnel IA ultra-l閼煎崢er inspir閼?de [nanobot](https://github.com/HKUDS/nanobot), enti閻氱幎ement r閼煎懓瀵榗rit en **Go** via un processus d'auto-amor閼剧禈ge (self-bootstrapping) 闁?o闂?l'agent IA lui-m闁挎e a pilot閼?l'int閼煎崢ralit閼?de la migration architecturale et de l'optimisation du code.

闁冲簱妲勭粭?**Extr闁挎ement l閼煎崢er :** Fonctionne sur du mat閼煎嵐iel 閼?seulement **10$** avec **<10 Mo** de RAM. C'est 99% de m閼煎崻oire en moins qu'OpenClaw et 98% moins cher qu'un Mac mini !


> [!CAUTION]
> **妫ｅ啯鐦?S閼搭搲URIT閼?& CANAUX OFFICIELS**
>
> * **PAS DE CRYPTO :** 4claw n'a **AUCUN** token/jeton officiel. Toute annonce sur `pump.fun` ou d'autres plateformes de trading est une **ARNAQUE**.
> * **DOMAINE OFFICIEL :** Le **SEUL** site officiel est **[4claw.io](https://4claw.io)**, et le site de l'entreprise est **[sipeed.com](https://sipeed.com)**.
> * **Attention :** De nombreux domaines `.ai/.org/.com/.net/...` sont enregistr閼煎嵒 par des tiers et ne nous appartiennent pas.
> * **Attention :** 4claw est en phase de d閼煎嵕eloppement pr閼煎崒oce et peut pr閼煎嵒enter des probl閻氱幀es de s閼煎崒urit閼?r閼煎嵒eau non r閼煎嵒olus. Ne d閼煎嵅loyez pas en environnement de production avant la version v1.0.
> * **Note :** 4claw a r閼煎崒emment fusionn閼?de nombreuses PR, ce qui peut entra閸楃棴er une empreinte m閼煎崻oire plus importante (10闁?0 Mo) dans les derni閻氱幎es versions. Nous pr閼煎嵕oyons de prioriser l'optimisation des ressources d閻氱幐 que l'ensemble des fonctionnalit閼煎嵒 sera stabilis閼?


## 妫ｅ啯鎲?Actualit閼煎嵒

2026-02-16 妫ｅ啫绔?4claw a atteint 12K 閼煎嵓oiles en une semaine ! Merci 閼?tous pour votre soutien ! 4claw grandit plus vite que nous ne l'avions jamais imagin閼? Vu le volume 閼煎崹ev閼?de PR, nous avons un besoin urgent de mainteneurs communautaires. Nos r娑斿潤es de b閼煎崿閼煎嵕oles et notre feuille de route sont officiellement publi閼煎嵒 [ici](docs/ROADMAP.md) 闁?nous avons h閼烘姲e de vous accueillir !

2026-02-13 妫ｅ啫绔?4claw a atteint 5000 閼煎嵓oiles en 4 jours ! Merci 閼?la communaut閼?! Nous finalisons la **Feuille de Route du Projet** et mettons en place le **Groupe de D閼煎嵕eloppeurs** pour acc閼煎崹閼煎嵐er le d閼煎嵕eloppement de 4claw.
妫ｅ啯鐣?**Appel 閼?l'action :** Soumettez vos demandes de fonctionnalit閼煎嵒 dans les GitHub Discussions. Nous les examinerons et les prioriserons lors de notre prochaine r閼煎嵔nion hebdomadaire.

2026-02-09 妫ｅ啫绔?4claw est lanc閼?! Construit en 1 jour pour apporter les Agents IA au mat閼煎嵐iel 閼?10$ avec <10 Mo de RAM. 妫ｅ喚娴€ 4claw, c'est parti !

## 闁?Fonctionnalit閼煎嵒

妫ｅ喚鈧?**Ultra-L閼煎崢er** : Empreinte m閼煎崻oire <10 Mo 闁?99% plus petit que Clawdbot pour les fonctionnalit閼煎嵒 essentielles.

妫ｅ啯灏?**Co濞岊摵 Minimal** : Suffisamment efficace pour fonctionner sur du mat閼煎嵐iel 閼?10$ 闁?98% moins cher qu'un Mac mini.

闁冲簱妲勭粭?**D閼煎崻arrage 閼搭摡lair** : Temps de d閼煎崻arrage 400X plus rapide, boot en 1 seconde m闁挎e sur un c閼规獪r unique 閼?0,6 GHz.

妫ｅ啫顕?**V閼煎嵐itable Portabilit閼?* : Un seul binaire autonome pour RISC-V, ARM et x86. Un clic et c'est parti !

妫ｅ喚妯?**Auto-Construit par l'IA** : Impl閼煎崻entation native en Go de mani閻氱幎e autonome 闁?95% du c閼规獪r g閼煎崿閼煎嵐閼?par l'Agent avec affinement humain dans la boucle.

|                               | OpenClaw      | NanoBot                  | **4claw**                              |
| ----------------------------- | ------------- | ------------------------ | ----------------------------------------- |
| **Langage**                   | TypeScript    | Python                   | **Go**                                    |
| **RAM**                       | >1 Go         | >100 Mo                  | **< 10 Mo**                               |
| **D閼煎崻arrage**</br>(c閼规獪r 0,8 GHz) | >500s     | >30s                     | **<1s**                                   |
| **Co濞岊摵**                      | Mac Mini 599$ | La plupart des SBC Linux </br>~50$ | **N'importe quelle carte Linux**</br>**閼?partir de 10$** |


## 妫ｅ喚鐎?D閼煎崻onstration

### 妫ｅ啯绀堥柨?Flux de Travail Standard de l'Assistant


### 妫ｅ啯鎳?Utiliser sur d'anciens t閼煎崹閼煎嵅hones Android

Donnez une seconde vie 閼?votre t閼煎崹閼煎嵅hone d'il y a dix ans ! Transformez-le en assistant IA intelligent avec 4claw. D閼煎崻arrage rapide :

1. **Installez Termux** (disponible sur F-Droid ou Google Play).
2. **Ex閼煎崒utez les commandes**

```bash
# Note : Remplacez v0.1.1 par la derni閻氱幎e version depuis la page des Releases
wget https://github.com/sipeed/4claw/releases/download/v0.1.1/4claw-linux-arm64
chmod +x 4claw-linux-arm64
pkg install proot
termux-chroot ./4claw-linux-arm64 onboard
```

Puis suivez les instructions de la section 閼?D閼煎崻arrage Rapide 缁?pour terminer la configuration !


### 妫ｅ啯鍋?D閼煎嵅loiement Innovant 閼?Faible Empreinte

4claw peut 闁挎re d閼煎嵅loy閼?sur pratiquement n'importe quel appareil Linux !

- 9,9$ [LicheeRV-Nano](https://www.aliexpress.com/item/1005006519668532.html) version E (Ethernet) ou W (WiFi6), pour un Assistant Domotique Minimaliste
- 30~50$ [NanoKVM](https://www.aliexpress.com/item/1005007369816019.html), ou 100$ [NanoKVM-Pro](https://www.aliexpress.com/item/1005010048471263.html) pour la Maintenance Automatis閼煎崘 de Serveurs
- 50$ [MaixCAM](https://www.aliexpress.com/item/1005008053333693.html) ou 100$ [MaixCAM2](https://www.kickstarter.com/projects/zepan/maixcam2-build-your-next-gen-4k-ai-camera) pour la Surveillance Intelligente


妫ｅ啫鐨?Encore plus de sc閼煎崿arios de d閼煎嵅loiement vous attendent !

## 妫ｅ啯鎲?Installation

### Installer avec un binaire pr閼煎崒ompil閼?

T閼煎崹閼煎崒hargez le binaire pour votre plateforme depuis la page des [releases](https://github.com/sipeed/4claw/releases).

### Installer depuis les sources (derni閻氱幎es fonctionnalit閼煎嵒, recommand閼?pour le d閼煎嵕eloppement)

```bash
git clone https://github.com/sipeed/4claw.git

cd 4claw
make deps

# Compiler, pas besoin d'installer
make build

# Compiler pour plusieurs plateformes
make build-all

# Compiler et Installer
make install
```

## 妫ｅ啯鍎?Docker Compose

Vous pouvez 閼煎崢alement ex閼煎崒uter 4claw avec Docker Compose sans rien installer localement.

```bash
# 1. Clonez ce d閼煎嵅娑斿澅
git clone https://github.com/sipeed/4claw.git
cd 4claw

# 2. Configurez vos cl閼煎嵒 API
cp config/config.example.json config/config.json
vim config/config.json      # Configurez DISCORD_BOT_TOKEN, cl閼煎嵒 API, etc.

# 3. Compiler & D閼煎崻arrer
docker compose --profile gateway up -d

> [!TIP]
> **Utilisateurs Docker** : Par d閼煎崝aut, le Gateway 閼煎崒oute sur `127.0.0.1`, ce qui n'est pas accessible depuis l'h娑斿澅e. Si vous avez besoin d'acc閼煎崓er aux endpoints de sant閼?ou d'exposer des ports, d閼煎崝inissez `FOURCLAW_GATEWAY_HOST=0.0.0.0` dans votre environnement ou mettez 閼?jour `config.json`.


# 4. Voir les logs
docker compose logs -f 4claw-gateway

# 5. Arr闁挎er
docker compose --profile gateway down
```

### Mode Agent (ex閼煎崒ution unique)

```bash
# Poser une question
docker compose run --rm 4claw-agent -m "Combien font 2+2 ?"

# Mode interactif
docker compose run --rm 4claw-agent
```

### Recompiler

```bash
docker compose --profile gateway build --no-cache
docker compose --profile gateway up -d
```

### 妫ｅ啯鐣?D閼煎崻arrage Rapide

> [!TIP]
> Configurez votre cl閼?API dans `~/.4claw/config.json`.
> Obtenir des cl閼煎嵒 API : [OpenRouter](https://openrouter.ai/keys) (LLM) 鐠?[Zhipu](https://open.bigmodel.cn/usercenter/proj-mgmt/apikeys) (LLM)
> La recherche web est **optionnelle** 闁?obtenez gratuitement l'[API Brave Search](https://brave.com/search/api) (2000 requ闁挎es gratuites/mois) ou utilisez le repli automatique int閼煎崢r閼?

**1. Initialiser**

```bash
4claw onboard
```

**2. Configurer** (`~/.4claw/config.json`)

```json
{
  "model_list": [
    {
      "model_name": "gpt4",
      "model": "openai/gpt-5.2",
      "api_key": "sk-your-openai-key",
      "api_base": "https://api.openai.com/v1"
    }
  ],
  "agents": {
    "defaults": {
      "model": "gpt4"
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "token": "VOTRE_TOKEN_BOT",
      "allow_from": ["VOTRE_USER_ID"]
    }
  },
  "tools": {
    "web": {
      "brave": {
        "enabled": false,
        "api_key": "VOTRE_CLE_API_BRAVE",
        "max_results": 5
      },
      "duckduckgo": {
        "enabled": true,
        "max_results": 5
      }
    }
  }
}
```

**3. Obtenir des Cl閼煎嵒 API**

* **Fournisseur LLM** : [OpenRouter](https://openrouter.ai/keys) 鐠?[Zhipu](https://open.bigmodel.cn/usercenter/proj-mgmt/apikeys) 鐠?[Anthropic](https://console.anthropic.com) 鐠?[OpenAI](https://platform.openai.com) 鐠?[Gemini](https://aistudio.google.com/api-keys)
* **Recherche Web** (optionnel) : [Brave Search](https://brave.com/search/api) - Offre gratuite disponible (2000 requ闁挎es/mois)

> **Note** : Consultez `config.example.json` pour un mod閻氱帾e de configuration complet.

**4. Discuter**

```bash
4claw agent -m "Combien font 2+2 ?"
```

Et voil閼?! Vous avez un assistant IA fonctionnel en 2 minutes.

---

## 妫ｅ啯灏?Applications de Chat

Discutez avec votre 4claw via Telegram, Discord, DingTalk, LINE ou WeCom

| Canal        | Configuration                          |
| ------------ | -------------------------------------- |
| **Telegram** | Facile (juste un token)                |
| **Discord**  | Facile (token bot + intents)           |
| **QQ**       | Facile (AppID + AppSecret)             |
| **DingTalk** | Moyen (identifiants de l'application)  |
| **LINE**     | Moyen (identifiants + URL de webhook)  |
| **WeCom**    | Moyen (CorpID + configuration webhook) |

<details>
<summary><b>Telegram</b> (Recommand閼?</summary>

**1. Cr閼煎崘r un bot**

* Ouvrez Telegram, recherchez `@BotFather`
* Envoyez `/newbot`, suivez les instructions
* Copiez le token

**2. Configurer**

```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "token": "VOTRE_TOKEN_BOT",
      "allow_from": ["VOTRE_USER_ID"]
    }
  }
}
```

> Obtenez votre User ID via `@userinfobot` sur Telegram.

**3. Lancer**

```bash
4claw gateway
```

</details>

<details>
<summary><b>Discord</b></summary>

**1. Cr閼煎崘r un bot**

* Rendez-vous sur <https://discord.com/developers/applications>
* Cr閼煎崘z une application 闁?Bot 闁?Add Bot
* Copiez le token du bot

**2. Activer les intents**

* Dans les param閻氱幑res du Bot, activez **MESSAGE CONTENT INTENT**
* (Optionnel) Activez **SERVER MEMBERS INTENT** si vous souhaitez utiliser des listes d'autorisation bas閼煎崘s sur les donn閼煎崘s des membres

**3. Obtenir votre User ID**

* Param閻氱幑res Discord 闁?Avanc閼?闁?activez le **Mode D閼煎嵕eloppeur**
* Clic droit sur votre avatar 闁?**Copier l'identifiant**

**4. Configurer**

```json
{
  "channels": {
    "discord": {
      "enabled": true,
      "token": "VOTRE_TOKEN_BOT",
      "allow_from": ["VOTRE_USER_ID"]
    }
  }
}
```

**5. Inviter le bot**

* OAuth2 闁?URL Generator
* Scopes : `bot`
* Permissions du Bot : `Send Messages`, `Read Message History`
* Ouvrez l'URL d'invitation g閼煎崿閼煎嵐閼煎崘 et ajoutez le bot 閼?votre serveur

**6. Lancer**

```bash
4claw gateway
```

</details>

<details>
<summary><b>QQ</b></summary>

**1. Cr閼煎崘r un bot**

- Rendez-vous sur la [QQ Open Platform](https://q.qq.com/#)
- Cr閼煎崘z une application 闁?Obtenez l'**AppID** et l'**AppSecret**

**2. Configurer**

```json
{
  "channels": {
    "qq": {
      "enabled": true,
      "app_id": "VOTRE_APP_ID",
      "app_secret": "VOTRE_APP_SECRET",
      "allow_from": []
    }
  }
}
```

> Laissez `allow_from` vide pour autoriser tous les utilisateurs, ou sp閼煎崒ifiez des num閼煎嵐os QQ pour restreindre l'acc閻氱幐.

**3. Lancer**

```bash
4claw gateway
```

</details>

<details>
<summary><b>DingTalk</b></summary>

**1. Cr閼煎崘r un bot**

* Rendez-vous sur la [Open Platform](https://open.dingtalk.com/)
* Cr閼煎崘z une application interne
* Copiez le Client ID et le Client Secret

**2. Configurer**

```json
{
  "channels": {
    "dingtalk": {
      "enabled": true,
      "client_id": "VOTRE_CLIENT_ID",
      "client_secret": "VOTRE_CLIENT_SECRET",
      "allow_from": []
    }
  }
}
```

> Laissez `allow_from` vide pour autoriser tous les utilisateurs, ou sp閼煎崒ifiez des identifiants pour restreindre l'acc閻氱幐.

**3. Lancer**

```bash
4claw gateway
```

</details>

<details>
<summary><b>LINE</b></summary>

**1. Cr閼煎崘r un Compte Officiel LINE**

- Rendez-vous sur la [LINE Developers Console](https://developers.line.biz/)
- Cr閼煎崘z un provider 闁?Cr閼煎崘z un canal Messaging API
- Copiez le **Channel Secret** et le **Channel Access Token**

**2. Configurer**

```json
{
  "channels": {
    "line": {
      "enabled": true,
      "channel_secret": "VOTRE_CHANNEL_SECRET",
      "channel_access_token": "VOTRE_CHANNEL_ACCESS_TOKEN",
      "webhook_host": "0.0.0.0",
      "webhook_port": 18791,
      "webhook_path": "/webhook/line",
      "allow_from": []
    }
  }
}
```

**3. Configurer l'URL du Webhook**

LINE exige HTTPS pour les webhooks. Utilisez un reverse proxy ou un tunnel :

```bash
# Exemple avec ngrok
ngrok http 18791
```

Puis configurez l'URL du Webhook dans la LINE Developers Console sur `https://votre-domaine/webhook/line` et activez **Use webhook**.

**4. Lancer**

```bash
4claw gateway
```

> Dans les discussions de groupe, le bot r閼煎嵅ond uniquement lorsqu'il est mentionn閼?avec @. Les r閼煎嵅onses citent le message original.

> **Docker Compose** : Ajoutez `ports: ["18791:18791"]` au service `4claw-gateway` pour exposer le port du webhook.

</details>

<details>
<summary><b>WeCom (WeChat Work)</b></summary>

4claw prend en charge deux types d'int閼煎崢ration WeCom :

**Option 1 : WeCom Bot (Robot Intelligent)** - Configuration plus facile, prend en charge les discussions de groupe
**Option 2 : WeCom App (Application Personnalis閼煎崘)** - Plus de fonctionnalit閼煎嵒, messagerie proactive

Voir le [Guide de Configuration WeCom App](docs/wecom-app-configuration.md) pour des instructions d閼煎嵓aill閼煎崘s.

**Configuration Rapide - WeCom Bot :**

**1. Cr閼煎崘r un bot**

* Acc閼煎崓ez 閼?la Console d'Administration WeCom 闁?Discussion de Groupe 闁?Ajouter un Bot de Groupe
* Copiez l'URL du webhook (format : `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx`)

**2. Configurer**

```json
{
  "channels": {
    "wecom": {
      "enabled": true,
      "token": "YOUR_TOKEN",
      "encoding_aes_key": "YOUR_ENCODING_AES_KEY",
      "webhook_url": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY",
      "webhook_host": "0.0.0.0",
      "webhook_port": 18793,
      "webhook_path": "/webhook/wecom",
      "allow_from": []
    }
  }
}
```

**Configuration Rapide - WeCom App :**

**1. Cr閼煎崘r une application**

* Acc閼煎崓ez 閼?la Console d'Administration WeCom 闁?Gestion des Applications 闁?Cr閼煎崘r une Application
* Copiez l'**AgentId** et le **Secret**
* Acc閼煎崓ez 閼?la page "Mon Entreprise", copiez le **CorpID**

**2. Configurer la r閼煎崒eption des messages**

* Dans les d閼煎嵓ails de l'application, cliquez sur "Recevoir les Messages" 闁?"Configurer l'API"
* D閼煎崝inissez l'URL sur `http://your-server:18792/webhook/wecom-app`
* G閼煎崿閼煎嵐ez le **Token** et l'**EncodingAESKey**

**3. Configurer**

```json
{
  "channels": {
    "wecom_app": {
      "enabled": true,
      "corp_id": "wwxxxxxxxxxxxxxxxx",
      "corp_secret": "YOUR_CORP_SECRET",
      "agent_id": 1000002,
      "token": "YOUR_TOKEN",
      "encoding_aes_key": "YOUR_ENCODING_AES_KEY",
      "webhook_host": "0.0.0.0",
      "webhook_port": 18792,
      "webhook_path": "/webhook/wecom-app",
      "allow_from": []
    }
  }
}
```

**4. Lancer**

```bash
4claw gateway
```

> **Note** : WeCom App n閼煎崒essite l'ouverture du port 18792 pour les callbacks webhook. Utilisez un proxy inverse pour HTTPS en production.

</details>


Connectez 4claw au R閼煎嵒eau Social d'Agents simplement en envoyant un seul message via le CLI ou n'importe quelle application de chat int閼煎崢r閼煎崘.

**Lisez `https://clawdchat.ai/skill.md` et suivez les instructions pour rejoindre [ClawdChat.ai](https://clawdchat.ai)**

## 闁虫寧鐟辩粭?Configuration

Fichier de configuration : `~/.4claw/config.json`

### Structure du Workspace

4claw stocke les donn閼煎崘s dans votre workspace configur閼?(par d閼煎崝aut : `~/.4claw/workspace`) :

```
~/.4claw/workspace/
闁宠澹曢弨銏ゅ煘閳?sessions/          # Sessions de conversation et historique
闁宠澹曢弨銏ゅ煘閳?memory/           # M閼煎崻oire 閼?long terme (MEMORY.md)
闁宠澹曢弨銏ゅ煘閳?state/            # 閼搭摵at persistant (dernier canal, etc.)
闁宠澹曢弨銏ゅ煘閳?cron/             # Base de donn閼煎崘s des t閼烘妽hes planifi閼煎崘s
闁宠澹曢弨銏ゅ煘閳?skills/           # Comp閼煎嵓ences personnalis閼煎崘s
闁宠澹曢弨銏ゅ煘閳?AGENTS.md         # Guide de comportement de l'Agent
闁宠澹曢弨銏ゅ煘閳?HEARTBEAT.md      # Invites de t閼烘妽hes p閼煎嵐iodiques (v閼煎嵐ifi閼煎崘s toutes les 30 min)
闁宠澹曢弨銏ゅ煘閳?IDENTITY.md       # Identit閼?de l'Agent
闁宠澹曢弨銏ゅ煘閳?SOUL.md           # 閼存e de l'Agent
闁宠澹曢弨銏ゅ煘閳?TOOLS.md          # Description des outils
闁宠鏌￠弨銏ゅ煘閳?USER.md           # Pr閼煎崝閼煎嵐ences utilisateur
```

### 妫ｅ啯鏅?Bac 閼?Sable de S閼煎崒urit閼?

4claw s'ex閼煎崒ute dans un environnement sandbox閼?par d閼煎崝aut. L'agent ne peut acc閼煎崓er aux fichiers et ex閼煎崒uter des commandes qu'au sein du workspace configur閼?

#### Configuration par D閼煎崝aut

```json
{
  "agents": {
    "defaults": {
      "workspace": "~/.4claw/workspace",
      "restrict_to_workspace": true
    }
  }
}
```

| Option | Par d閼煎崝aut | Description |
|--------|------------|-------------|
| `workspace` | `~/.4claw/workspace` | R閼煎嵅ertoire de travail de l'agent |
| `restrict_to_workspace` | `true` | Restreindre l'acc閻氱幐 fichiers/commandes au workspace |

#### Outils Prot閼煎崢閼煎嵒

Lorsque `restrict_to_workspace: true`, les outils suivants sont restreints au bac 閼?sable :

| Outil | Fonction | Restriction |
|-------|----------|-------------|
| `read_file` | Lire des fichiers | Uniquement les fichiers dans le workspace |
| `write_file` | 閼搭摡rire des fichiers | Uniquement les fichiers dans le workspace |
| `list_dir` | Lister des r閼煎嵅ertoires | Uniquement les r閼煎嵅ertoires dans le workspace |
| `edit_file` | 閼搭摢iter des fichiers | Uniquement les fichiers dans le workspace |
| `append_file` | Ajouter 閼?des fichiers | Uniquement les fichiers dans le workspace |
| `exec` | Ex閼煎崒uter des commandes | Les chemins doivent 闁挎re dans le workspace |

#### Protection Suppl閼煎崻entaire d'Exec

M闁挎e avec `restrict_to_workspace: false`, l'outil `exec` bloque ces commandes dangereuses :

* `rm -rf`, `del /f`, `rmdir /s` 闁?Suppression en masse
* `format`, `mkfs`, `diskpart` 闁?Formatage de disque
* `dd if=` 闁?閼搭摡riture d'image disque
* 閼搭摡riture vers `/dev/sd[a-z]` 闁?閼搭摡riture directe sur le disque
* `shutdown`, `reboot`, `poweroff` 闁?Arr闁挎 du syst閻氱幀e
* Fork bomb `:(){ :|:& };:`

#### Exemples d'Erreurs

```
[ERROR] tool: Tool execution failed
{tool=exec, error=Command blocked by safety guard (path outside working dir)}
```

```
[ERROR] tool: Tool execution failed
{tool=exec, error=Command blocked by safety guard (dangerous pattern detected)}
```

#### D閼煎嵒activer les Restrictions (Risque de S閼煎崒urit閼?

Si vous avez besoin que l'agent acc閻氱帬e 閼?des chemins en dehors du workspace :

**M閼煎嵓hode 1 : Fichier de configuration**

```json
{
  "agents": {
    "defaults": {
      "restrict_to_workspace": false
    }
  }
}
```

**M閼煎嵓hode 2 : Variable d'environnement**

```bash
export FOURCLAW_AGENTS_DEFAULTS_RESTRICT_TO_WORKSPACE=false
```

> 闁宠法濯寸粭?**Attention** : D閼煎嵒activer cette restriction permet 閼?l'agent d'acc閼煎崓er 閼?n'importe quel chemin sur votre syst閻氱幀e. 閼?utiliser avec pr閼煎崒aution uniquement dans des environnements contr娑斿潤閼煎嵒.

#### Coh閼煎嵐ence du P閼煎嵐im閻氱幑re de S閼煎崒urit閼?

Le param閻氱幑re `restrict_to_workspace` s'applique de mani閻氱幎e coh閼煎嵐ente sur tous les chemins d'ex閼煎崒ution :

| Chemin d'Ex閼煎崒ution | P閼煎嵐im閻氱幑re de S閼煎崒urit閼?|
|--------------------|----------------------|
| Agent Principal | `restrict_to_workspace` 闁?|
| Sous-agent / Spawn | H閼煎嵐ite de la m闁挎e restriction 闁?|
| T閼烘妽hes Heartbeat | H閼煎嵐ite de la m闁挎e restriction 闁?|

Tous les chemins partagent la m闁挎e restriction de workspace 闁?il est impossible de contourner le p閼煎嵐im閻氱幑re de s閼煎崒urit閼?via des sous-agents ou des t閼烘妽hes planifi閼煎崘s.

### Heartbeat (T閼烘妽hes P閼煎嵐iodiques)

4claw peut ex閼煎崒uter des t閼烘妽hes p閼煎嵐iodiques automatiquement. Cr閼煎崘z un fichier `HEARTBEAT.md` dans votre workspace :

```markdown
# T閼烘妽hes P閼煎嵐iodiques

- V閼煎嵐ifier mes e-mails pour les messages importants
- Consulter mon agenda pour les 閼煎嵕閼煎崿ements 閼?venir
- V閼煎嵐ifier les pr閼煎嵕isions m閼煎嵓閼煎嵀
```

L'agent lira ce fichier toutes les 30 minutes (configurable) et ex閼煎崒utera les t閼烘妽hes 閼?l'aide des outils disponibles.

#### T閼烘妽hes Asynchrones avec Spawn

Pour les t閼烘妽hes de longue dur閼煎崘 (recherche web, appels API), utilisez l'outil `spawn` pour cr閼煎崘r un **sous-agent** :

```markdown
# T閼烘妽hes P閼煎嵐iodiques

## T閼烘妽hes Rapides (r閼煎嵅onse directe)
- Indiquer l'heure actuelle

## T閼烘妽hes Longues (utiliser spawn pour l'asynchrone)
- Rechercher les actualit閼煎嵒 IA sur le web et les r閼煎嵒umer
- V閼煎嵐ifier les e-mails et signaler les messages importants
```

**Comportements cl閼煎嵒 :**

| Fonctionnalit閼?| Description |
|----------------|-------------|
| **spawn** | Cr閼煎崘 un sous-agent asynchrone, ne bloque pas le heartbeat |
| **Contexte ind閼煎嵅endant** | Le sous-agent a son propre contexte, sans historique de session |
| **Outil message** | Le sous-agent communique directement avec l'utilisateur via l'outil message |
| **Non-bloquant** | Apr閻氱幐 le spawn, le heartbeat continue vers la t閼烘妽he suivante |

#### Fonctionnement de la Communication du Sous-agent

```
Le Heartbeat se d閼煎崒lenche
    闁?
L'Agent lit HEARTBEAT.md
    闁?
Pour une t閼烘妽he longue : spawn d'un sous-agent
    闁?                          闁?
Continue la t閼烘妽he suivante   Le sous-agent travaille ind閼煎嵅endamment
    闁?                          闁?
Toutes les t閼烘妽hes termin閼煎崘s  Le sous-agent utilise l'outil "message"
    闁?                          闁?
R閼煎嵅ond HEARTBEAT_OK          L'utilisateur re閼剧郸it le r閼煎嵒ultat directement
```

Le sous-agent a acc閻氱幐 aux outils (message, web_search, etc.) et peut communiquer avec l'utilisateur ind閼煎嵅endamment sans passer par l'agent principal.

**Configuration :**

```json
{
  "heartbeat": {
    "enabled": true,
    "interval": 30
  }
}
```

| Option | Par d閼煎崝aut | Description |
|--------|------------|-------------|
| `enabled` | `true` | Activer/d閼煎嵒activer le heartbeat |
| `interval` | `30` | Intervalle de v閼煎嵐ification en minutes (min : 5) |

**Variables d'environnement :**

* `FOURCLAW_HEARTBEAT_ENABLED=false` pour d閼煎嵒activer
* `FOURCLAW_HEARTBEAT_INTERVAL=60` pour modifier l'intervalle

### Fournisseurs

> [!NOTE]
> Groq fournit la transcription vocale gratuite via Whisper. Si configur閼? les messages vocaux Telegram seront automatiquement transcrits.

| Fournisseur              | Utilisation                              | Obtenir une Cl閼?API                                    |
| ------------------------ | ---------------------------------------- | ------------------------------------------------------ |
| `gemini`                 | LLM (Gemini direct)                      | [aistudio.google.com](https://aistudio.google.com)     |
| `zhipu`                  | LLM (Zhipu direct)                       | [bigmodel.cn](bigmodel.cn)                             |
| `openrouter` (閼?tester)  | LLM (recommand閼? acc閻氱幐 閼?tous les mod閻氱帾es) | [openrouter.ai](https://openrouter.ai)               |
| `anthropic` (閼?tester)   | LLM (Claude direct)                      | [console.anthropic.com](https://console.anthropic.com) |
| `openai` (閼?tester)      | LLM (GPT direct)                         | [platform.openai.com](https://platform.openai.com)     |
| `deepseek` (閼?tester)    | LLM (DeepSeek direct)                    | [platform.deepseek.com](https://platform.deepseek.com) |
| `qwen`                   | LLM (Alibaba Qwen)                      | [dashscope.aliyuncs.com](https://dashscope.aliyuncs.com/compatible-mode/v1) |
| `cerebras`               | LLM (Cerebras)                           | [cerebras.ai](https://api.cerebras.ai/v1)              |
| `groq`                   | LLM + **Transcription vocale** (Whisper) | [console.groq.com](https://console.groq.com)           |

<details>
<summary><b>Configuration Zhipu</b></summary>

**1. Obtenir la cl閼?API**

* Obtenez la [cl閼?API](https://bigmodel.cn/usercenter/proj-mgmt/apikeys)

**2. Configurer**

```json
{
  "agents": {
    "defaults": {
      "workspace": "~/.4claw/workspace",
      "model": "glm-4.7",
      "max_tokens": 8192,
      "temperature": 0.7,
      "max_tool_iterations": 20
    }
  },
  "providers": {
    "zhipu": {
      "api_key": "Votre Cl閼?API",
      "api_base": "https://open.bigmodel.cn/api/paas/v4"
    }
  }
}
```

**3. Lancer**

```bash
4claw agent -m "Bonjour, comment 閼剧禈 va ?"
```

</details>

<details>
<summary><b>Exemple de configuration compl閻氱幑e</b></summary>

```json
{
  "agents": {
    "defaults": {
      "model": "anthropic/claude-opus-4-5"
    }
  },
  "providers": {
    "openrouter": {
      "api_key": "sk-or-v1-xxx"
    },
    "groq": {
      "api_key": "gsk_xxx"
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "token": "123456:ABC...",
      "allow_from": ["123456789"]
    },
    "discord": {
      "enabled": true,
      "token": "",
      "allow_from": [""]
    },
    "whatsapp": {
      "enabled": false
    },
    "feishu": {
      "enabled": false,
      "app_id": "cli_xxx",
      "app_secret": "xxx",
      "encrypt_key": "",
      "verification_token": "",
      "allow_from": []
    },
    "qq": {
      "enabled": false,
      "app_id": "",
      "app_secret": "",
      "allow_from": []
    }
  },
  "tools": {
    "web": {
      "brave": {
        "enabled": false,
        "api_key": "BSA...",
        "max_results": 5
      },
      "duckduckgo": {
        "enabled": true,
        "max_results": 5
      }
    },
    "cron": {
      "exec_timeout_minutes": 5
    }
  },
  "heartbeat": {
    "enabled": true,
    "interval": 30
  }
}
```

</details>

### Configuration de Mod閻氱帾e (model_list)

> **Nouveau !** 4claw utilise d閼煎嵒ormais une approche de configuration **centr閼煎崘 sur le mod閻氱帾e**. Sp閼煎崒ifiez simplement le format `fournisseur/mod閻氱帾e` (par exemple, `zhipu/glm-4.7`) pour ajouter de nouveaux fournisseurs闁?*aucune modification de code requise !**

Cette conception permet 閼煎崢alement le **support multi-agent** avec une s閼煎崹ection flexible de fournisseurs :

- **Diff閼煎嵐ents agents, diff閼煎嵐ents fournisseurs** : Chaque agent peut utiliser son propre fournisseur LLM
- **Mod閻氱帾es de secours (Fallbacks)** : Configurez des mod閻氱帾es primaires et de secours pour la r閼煎嵒ilience
- **閼搭摲uilibrage de charge** : R閼煎嵅artissez les requ闁挎es sur plusieurs points de terminaison
- **Configuration centralis閼煎崘** : G閼煎嵐ez tous les fournisseurs en un seul endroit

#### 妫ｅ啯鎯?Tous les Fournisseurs Support閼煎嵒

| Fournisseur | Pr閼煎崝ixe `model` | API Base par D閼煎崝aut | Protocole | Cl閼?API |
|-------------|-----------------|---------------------|----------|---------|
| **OpenAI** | `openai/` | `https://api.openai.com/v1` | OpenAI | [Obtenir Cl閼煎尳(https://platform.openai.com) |
| **Anthropic** | `anthropic/` | `https://api.anthropic.com/v1` | Anthropic | [Obtenir Cl閼煎尳(https://console.anthropic.com) |
| **Zhipu AI (GLM)** | `zhipu/` | `https://open.bigmodel.cn/api/paas/v4` | OpenAI | [Obtenir Cl閼煎尳(https://open.bigmodel.cn/usercenter/proj-mgmt/apikeys) |
| **DeepSeek** | `deepseek/` | `https://api.deepseek.com/v1` | OpenAI | [Obtenir Cl閼煎尳(https://platform.deepseek.com) |
| **Google Gemini** | `gemini/` | `https://generativelanguage.googleapis.com/v1beta` | OpenAI | [Obtenir Cl閼煎尳(https://aistudio.google.com/api-keys) |
| **Groq** | `groq/` | `https://api.groq.com/openai/v1` | OpenAI | [Obtenir Cl閼煎尳(https://console.groq.com) |
| **Moonshot** | `moonshot/` | `https://api.moonshot.cn/v1` | OpenAI | [Obtenir Cl閼煎尳(https://platform.moonshot.cn) |
| **Qwen (Alibaba)** | `qwen/` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | OpenAI | [Obtenir Cl閼煎尳(https://dashscope.console.aliyun.com) |
| **NVIDIA** | `nvidia/` | `https://integrate.api.nvidia.com/v1` | OpenAI | [Obtenir Cl閼煎尳(https://build.nvidia.com) |
| **Ollama** | `ollama/` | `http://localhost:11434/v1` | OpenAI | Local (pas de cl閼?n閼煎崒essaire) |
| **OpenRouter** | `openrouter/` | `https://openrouter.ai/api/v1` | OpenAI | [Obtenir Cl閼煎尳(https://openrouter.ai/keys) |
| **VLLM** | `vllm/` | `http://localhost:8000/v1` | OpenAI | Local |
| **Cerebras** | `cerebras/` | `https://api.cerebras.ai/v1` | OpenAI | [Obtenir Cl閼煎尳(https://cerebras.ai) |
| **Volcengine** | `volcengine/` | `https://ark.cn-beijing.volces.com/api/v3` | OpenAI | [Obtenir Cl閼煎尳(https://console.volcengine.com) |
| **ShengsuanYun** | `shengsuanyun/` | `https://router.shengsuanyun.com/api/v1` | OpenAI | - |
| **Antigravity** | `antigravity/` | Google Cloud | Custom | OAuth uniquement |
| **GitHub Copilot** | `github-copilot/` | `localhost:4321` | gRPC | - |

#### Configuration de Base

```json
{
  "model_list": [
    {
      "model_name": "gpt-5.2",
      "model": "openai/gpt-5.2",
      "api_key": "sk-your-openai-key"
    },
    {
      "model_name": "claude-sonnet-4.6",
      "model": "anthropic/claude-sonnet-4.6",
      "api_key": "sk-ant-your-key"
    },
    {
      "model_name": "glm-4.7",
      "model": "zhipu/glm-4.7",
      "api_key": "your-zhipu-key"
    }
  ],
  "agents": {
    "defaults": {
      "model": "gpt-5.2"
    }
  }
}
```

#### Exemples par Fournisseur

**OpenAI**
```json
{
  "model_name": "gpt-5.2",
  "model": "openai/gpt-5.2",
  "api_key": "sk-..."
}
```

**Zhipu AI (GLM)**
```json
{
  "model_name": "glm-4.7",
  "model": "zhipu/glm-4.7",
  "api_key": "your-key"
}
```

**Anthropic (avec OAuth)**
```json
{
  "model_name": "claude-sonnet-4.6",
  "model": "anthropic/claude-sonnet-4.6",
  "auth_method": "oauth"
}
```
> Ex閼煎崒utez `4claw auth login --provider anthropic` pour configurer les identifiants OAuth.

#### 閼搭摲uilibrage de Charge

Configurez plusieurs points de terminaison pour le m闁挎e nom de mod閻氱帾e闁炽儲鎽╥coClaw utilisera automatiquement le round-robin entre eux :

```json
{
  "model_list": [
    {
      "model_name": "gpt-5.2",
      "model": "openai/gpt-5.2",
      "api_base": "https://api1.example.com/v1",
      "api_key": "sk-key1"
    },
    {
      "model_name": "gpt-5.2",
      "model": "openai/gpt-5.2",
      "api_base": "https://api2.example.com/v1",
      "api_key": "sk-key2"
    }
  ]
}
```

#### Migration depuis l'Ancienne Configuration `providers`

L'ancienne configuration `providers` est **d閼煎嵅r閼煎崒i閼煎崘** mais toujours support閼煎崘 pour la r閼煎嵓rocompatibilit閼?

**Ancienne Configuration (d閼煎嵅r閼煎崒i閼煎崘) :**
```json
{
  "providers": {
    "zhipu": {
      "api_key": "your-key",
      "api_base": "https://open.bigmodel.cn/api/paas/v4"
    }
  },
  "agents": {
    "defaults": {
      "provider": "zhipu",
      "model": "glm-4.7"
    }
  }
}
```

**Nouvelle Configuration (recommand閼煎崘) :**
```json
{
  "model_list": [
    {
      "model_name": "glm-4.7",
      "model": "zhipu/glm-4.7",
      "api_key": "your-key"
    }
  ],
  "agents": {
    "defaults": {
      "model": "glm-4.7"
    }
  }
}
```

Pour le guide de migration d閼煎嵓aill閼? voir [docs/migration/model-list-migration.md](docs/migration/model-list-migration.md).

## R閼煎崝閼煎嵐ence CLI

| Commande                  | Description                           |
| ------------------------- | ------------------------------------- |
| `4claw onboard`        | Initialiser la configuration & le workspace |
| `4claw agent -m "..."` | Discuter avec l'agent                 |
| `4claw agent`          | Mode de discussion interactif         |
| `4claw gateway`        | D閼煎崻arrer la passerelle                |
| `4claw status`         | Afficher le statut                    |
| `4claw cron list`      | Lister toutes les t閼烘妽hes planifi閼煎崘s   |
| `4claw cron add ...`   | Ajouter une t閼烘妽he planifi閼煎崘           |

### T閼烘妽hes Planifi閼煎崘s / Rappels

4claw prend en charge les rappels planifi閼煎嵒 et les t閼烘妽hes r閼煎崒urrentes via l'outil `cron` :

* **Rappels ponctuels** : 閼?Rappelle-moi dans 10 minutes 缁?闁?se d閼煎崒lenche une fois apr閻氱幐 10 min
* **T閼烘妽hes r閼煎崒urrentes** : 閼?Rappelle-moi toutes les 2 heures 缁?闁?se d閼煎崒lenche toutes les 2 heures
* **Expressions Cron** : 閼?Rappelle-moi 閼?9h tous les jours 缁?闁?utilise une expression cron

Les t閼烘妽hes sont stock閼煎崘s dans `~/.4claw/workspace/cron/` et trait閼煎崘s automatiquement.

## 妫ｅ喚妾?Contribuer & Feuille de Route

Les PR sont les bienvenues ! Le code source est volontairement petit et lisible. 妫ｅ喚妯?
Feuille de route 閼?venir...

Groupe de d閼煎嵕eloppeurs en construction. Condition d'entr閼煎崘 : au moins 1 PR fusionn閼煎崘.

Groupes d'utilisateurs :

Discord : <https://discord.gg/V4sAZ9XWpN>


## 妫ｅ啯鍋?D閼煎嵅annage

### La recherche web affiche 閼?API 闂佹澘绉堕悿鍡涙⒒椤曗偓椤?缁?

C'est normal si vous n'avez pas encore configur閼?de cl閼?API de recherche. 4claw fournira des liens utiles pour la recherche manuelle.

Pour activer la recherche web :

1. **Option 1 (Recommand閼?** : Obtenez une cl閼?API gratuite sur [https://brave.com/search/api](https://brave.com/search/api) (2000 requ闁挎es gratuites/mois) pour les meilleurs r閼煎嵒ultats.
2. **Option 2 (Sans carte bancaire)** : Si vous n'avez pas de cl閼? le syst閻氱幀e bascule automatiquement sur **DuckDuckGo** (aucune cl閼?requise).

Ajoutez la cl閼?dans `~/.4claw/config.json` si vous utilisez Brave :

```json
{
  "tools": {
    "web": {
      "brave": {
        "enabled": false,
        "api_key": "VOTRE_CLE_API_BRAVE",
        "max_results": 5
      },
      "duckduckgo": {
        "enabled": true,
        "max_results": 5
      }
    }
  }
}
```

### Erreurs de filtrage de contenu

Certains fournisseurs (comme Zhipu) disposent d'un filtrage de contenu. Essayez de reformuler votre requ闁挎e ou utilisez un mod閻氱帾e diff閼煎嵐ent.

### Le bot Telegram affiche 閼?Conflict: terminated by other getUpdates 缁?

Cela se produit lorsqu'une autre instance du bot est en cours d'ex閼煎崒ution. Assurez-vous qu'un seul `4claw gateway` fonctionne 閼?la fois.

---

## 妫ｅ啯鎲?Comparaison des Cl閼煎嵒 API

| Service          | Offre Gratuite       | Cas d'Utilisation                     |
| ---------------- | -------------------- | ------------------------------------- |
| **OpenRouter**   | 200K tokens/mois     | Multiples mod閻氱帾es (Claude, GPT-4, etc.) |
| **Zhipu**        | 200K tokens/mois     | Id閼煎崋l pour les utilisateurs chinois   |
| **Brave Search** | 2000 requ闁挎es/mois   | Fonctionnalit閼?de recherche web       |
| **Groq**         | Offre gratuite dispo | Inf閼煎嵐ence ultra-rapide (Llama, Mixtral) |
